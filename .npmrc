# This machine has NODE_ENV=production and a user-level npm config of `omit=dev`,
# which makes a plain `npm install` silently skip devDependencies — vite never
# installs and `npm run dev` fails with "vite is not recognized".
#
# `include` wins over `omit`, so this forces the build toolchain to install
# regardless of the ambient environment.
include=dev
